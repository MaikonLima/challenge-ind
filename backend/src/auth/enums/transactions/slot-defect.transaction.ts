const default_value = 800

export const SlotDefectTransaction = {
    CREATE: [default_value],
    READ: [default_value],
    READ_BY_CABINET_ID: [default_value],
    CHANGE_STATUS: [default_value],
}