const default_value = 900

export const SerialExceptionTransaction = {
    APPROVED_SERIAL: [default_value],
    GET_WORK_ORDER_BY_SERIAL: [default_value],
    CREATE_SERIAL_EXCEPTION: [default_value],
    READ_SERIAL_EXCEPTION: [default_value],
    GET_MES_USER: [default_value],
    VERIFY_ADAPTER_SERIAL: [default_value]
}